spring:
  datasource:
    username: sa
    password:
    url:
    driver-class-name: org.h2.Driver


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      defalut_batch_fetch_size: 10
      hibernate:
        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug